# Copy this file to .env and fill in your key.
#
#   cp .env.example .env
#
# This file IS committed to git — keep it a placeholder, never a real key.
# .env is gitignored; that is where the real key belongs.
# Register an agent at https://api.llm4agents.com/docs to get one.

LLM4AGENTS_API_KEY=
